import React from 'react';

class Snap extends React.Component{

    constructor(props){
        super(props);
        this.state = {
            // add in the empty player deck and the empty computer deck. Use a method to take the deck from App passed 
            // down in props to player and comp. deck. Use shuffle method from props. 
        }
    }

    render(){
        return(
            <div>
            </div>
        )
    }

}

export default Snap;